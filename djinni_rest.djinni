thread_launcher = interface +o +j {
    # name is nullable
    start_thread(name: string, run_fn: async_task);
}

async_task = interface +c {
    execute();
}

api = interface +c {
    static create_api(launcher: thread_launcher): api;

    do_something();
}